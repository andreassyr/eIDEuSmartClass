version: '2'
services:
    smartClassApp:
        container_name: smartClassApp
        image: endimion13/eidas-gr-smartclass:0.2
        expose:
            - 8090
        ports:
            - 8090:8090
        environment:
            - KEY=secret
            - RASPBERRY_URL=http://test.com
            - RASPBERRY_CLOSE_ROOM=closeroom
        volumes:
            
